import React from 'react';
import PropTypes from 'prop-types';
import ArtistCard from '../artists/ArtistCard';

const AlbumCard = ({ title, id, }) => {

return(

)

};

AlbumCard.propTypes = {

};

export default ArtistCard;